# tyane-dash / 作業ルール（必ず最初に読む）

## 最終目標
プロップファームの審査に合格し、プロップトレーダーとして活動する。
現在の主タスク：バイナリー用ロジックを再精査し、FXで運用可能か検証する。

## Claude の動き方（厳守）
1. **推測で先走らない。** 依頼が曖昧なら手を動かす前に確認する。
2. **まず認識合わせ。** 大きい作業は「理解のまとめ＋質問」を先に出し、承認後に着手。
3. **簡潔に。** 長文は読まれない。結論→根拠の順、箇条書き主体。
4. **途中でコメントが来たら**、作業を止めて何を聞かれているか確認する。
5. **消費を抑える。** 不要な探索・全文読み込みをしない（下記参照）。
6. **事実と推測を分ける。** 未確認は「未確認」と書く。検証していないことを断定しない。

## 消費を抑えるルール
- `index.html` の `var D={...}`（巨大JSON）は読まない。必要なら行範囲を絞る。
- ファイル全体ではなく grep / 行範囲指定で読む。
- 調査を始める前に、対象ファイルが分かっているなら直接開く。

## セッション管理
- 作業は `sessions/<連番>-<短い名前>/` 単位で行う。
- 各フォルダの `CLAUDE.md` にそのセッション固有のルールを書く（このファイルより優先）。
- 決定・結果は各セッションの `NOTES.md` に追記する。会話ではなくファイルに残す。
- 新規セッションは `sessions/_template/` をコピーして作る。

## 環境の制約（確認済みの事実）
- ここは**クラウド上の使い捨てコンテナ**。ローカルPCのファイルは見えない。
- MQLのロジック本体とTICKデータは**ユーザーのローカルPCにある**（未取得）。
- `note.com` はネットワーク制限で**アクセス不可**。記事は本文をコピペしてもらう。
- このリポジトリの中身は `index.html` 1枚のみ（集計済み勝敗データ＋表示処理）。売買ロジックは含まれない。

## リポジトリ
- `index.html` … TyanePon 勝率ダッシュボード。単体HTML、ビルド不要。
- 作業ブランチ： `claude/aiajan-usability-t2w38v`
