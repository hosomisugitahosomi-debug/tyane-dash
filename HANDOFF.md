# 引き継ぎ（このファイルを最初に読む）

最終更新：2026-08-20

## 最終目標
**FundedElite** の審査に合格し、プロップトレーダーとして活動する。

## 今のタスク
バイナリー用ロジック（MQL化済み・3分判定）を、FXで運用できるか検証する。

---

## ★ 未解決の核心

**ロジックの中身が分かっていない。**
このリポジトリに売買ロジックは含まれておらず、入っているのは日別の勝敗数（集計済み）だけ。
エントリー条件も、1トレードで何pips取っているかも不明。

**MQLソースとTICKデータはローカルPCにある。** ここから始める。

---

## 確定した事実（公式FAQ・サポート回答）

### 保有時間とスキャルピング判定 ← 最重要
- **最低保有時間：通常口座3分 / スキャルピングアドオン付き30秒**
- **3分「未満」の決済はすべてスキャルピング扱い**
- チャレンジ中：利益の小さいスキャル1〜2回は見逃される可能性。それ以上は不合格
- **Funded段階：1回でも分配減額・口座リセットになり得る**
- スキャル取引の合計利益が上限超過＝**ハードブリーチ（口座終了・利益没収）**
- 上限は口座サイズ連動。**$10,000口座で$150（＝1.5%）**
- HFTの定義はSEC基準（コロケーション、大量発注と即時取消等）。
  公式FAQは**「3分以上保有すればHFTフラグを回避できる」**と位置づけている

> ### ⛔ 決定事項
> **決済は「3分ちょうど」ではなく「3分＋余裕（3分10〜30秒）」にする。**
> 約定遅延・サーバー時刻差でわずかでも3分を切ると即スキャル判定になるため。
> **検証もこの時間で行うこと**（「3分後」ではなく「3分20秒後」の値幅を測る）。
>
> スキャルピングアドオン（+40%・乗算）は**不要**。決済を後ろにずらせば無料で同じ効果。

### その他の確定事項
| 項目 | 内容 |
|---|---|
| EAの使用 | **許可**。ただし共有EA（他人と同一EAでの一斉売買）は不可 |
| 戦略リスク制限 | **プロ口座のみ適用。チャレンジ中は適用なし。** 同一取引日・単一銘柄で日次損失限度額の50%超のリスク禁止 |
| リスクの計算 | SL未設定なら保有中の最大逆行幅、SL設定時はエントリーから最も離れたSL水準。**利益決済でも計上**。同一銘柄・同一日で合算され、**決済しても枠は戻らない** |
| ソフトブリーチ | 3分未満の決済は勝ちでも該当。**分配30%に減額＋出金サイクルがリセット**（英語版FAQのみ定義） |
| ヘッジング | 全面禁止（同一口座内・自分の複数口座間・他社との間もすべて） |
| プラットフォーム | MetaTrader5 または TradeLocker |
| 取引銘柄 | `GBPJPYc` `EURJPYc` `USDJPYc` あり。`BTCUSD` あり（`.c` なし）。**`.c` 付きが最低スプレッド** |

### プラン別の損失限度額
| プラン | 日次(DLL) | 最大(MLL) | DD方式 |
|---|---|---|---|
| LITE 1ステップ | 3% | 8%（可変） | STATIC |
| LITE 2ステップ | 4% | 8%（可変） | MLLはSTATIC / DLLはEOD |
| 1ステップ+リトライ | 3% | 8%（可変） | STATIC |
| インスタント | 3% | 6% | トレーリング |
| フラッシュ | — | — | 評価フェーズはSTATIC |

詳細 → `docs/fundedelite-rules.md` ／ 費用 → `docs/cost.md`

---

## ★ ローカルで測るべき数字

> **決済タイミング（3分20秒など）における、1トレードあたりの平均獲得値幅（pips）**

これが合格ラインを超えれば計画は成立、超えなければ成立しない。

判定式：`実効勝率 p_eff = 実測勝率 - 往復コスト ÷ (2 × 平均値幅)`
これが下の「必要勝率」を上回れば、利益目標12%に到達できる。

| ペア | 実測勝率 | 必要勝率 | 余裕 |
|---|---|---|---|
| ポン円 | 64.3% | 60.2% | +4.1pt |
| ビットドル | 58.3% | 55.3% | +3.0pt |
| ユロ円 | 61.4% | 59.4% | +2.0pt |
| ドル円 | 54.5% | 59.5% | **-5.0pt（除外）** |

計算のやり直しは `sessions/01-binary-logic/scripts/breakeven.py` を実行。

> ⚠️ この表は**勝敗数のみからRR 1:1を仮定した試算**。実測値が出たら作り直すこと。
> 詳細は `sessions/01-binary-logic/REPORT-01-prop-fit.md` `REPORT-02-breakeven.md`。

---

## 進める順番
1. **MQLソース（.mq4/.mq5）とTICKデータの場所とサイズを特定**（中身はまだ読まない）
2. TICKデータの形式を確認（CSVか、MT5独自バイナリか）
3. **MQLソースからエントリー・エグジット条件を明文化** → `sessions/01-binary-logic/NOTES.md` に記録
4. **決済時刻を3分20秒に設定した場合の平均獲得値幅を実測**（まず1ペア・3ヶ月に絞る）
5. スプレッド＋スリッページ＋手数料を引いた期待値を出す
6. 上の判定式に入れて合否を出す

## 未取得の情報
- [ ] **GBPJPYc / EURJPYc / BTCUSD の平均スプレッド**（サポートに問い合わせ中）
- [ ] FX手数料$3の適用範囲（スポットFXか）と単位（片道／往復・1ロットあたり）
- [ ] LITE・インスタントの正確な価格（購入画面でのみ確定）
- [ ] MLLをカスタマイズで何%まで広げられるか

---

## 進め方のルール
ルートの `CLAUDE.md` に記載。「先走らない・まず認識合わせ・簡潔に・消費を抑える」。

**消費を抑えるための鉄則**
- TICKデータの中身を直接読まない。集計スクリプト経由で結果だけ受け取る
- まず小さい範囲で試し、手順が固まってから全期間へ
- 一度集計したら中間ファイルに保存し、再集計しない

## ファイル構成
```
CLAUDE.md                    作業ルール（毎回自動で読まれる）
HANDOFF.md                   このファイル
TODO.md                      ユーザーがやること
docs/
  fundedelite-rules.md       プロップのルール全部（確認状況つき）
  cost.md                    費用まとめ
  questions-to-fundedelite.md サポートへの質問と回答
  local-setup.md             ローカル移行手順
sessions/01-binary-logic/
  CLAUDE.md                  このセッション専用ルール
  NOTES.md                   作業ログ（決定事項はここに追記）
  REPORT-01-prop-fit.md      試算：プロップ条件との突き合わせ
  REPORT-02-breakeven.md     試算：損益分岐スプレッド早見表
  scripts/                   検証スクリプト
index.html                   勝率ダッシュボード（今回は改修対象外）
```
